DATABASE_URL=postgresql://USUARIO:SENHA_FORTE@localhost:5433/sistema?schema=public

# Auth — JWT_SECRET DEVE ter 32+ caracteres. Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=GERE_UM_SECRET_FORTE_COM_O_COMANDO_ACIMA_MIN_32_CHARS
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=604800

# App
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000
